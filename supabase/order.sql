-- ============================================================================
-- Protoboard — порядок задач (выполнить ОДИН раз, ДО публикации)
--
-- Меняем тип sort_order на дробный, чтобы при перестановке задачи можно было
-- вставлять её «между» соседями одним действием (без перенумерации всей колонки).
-- Данные сохраняются (целые превратятся в дробные). Безопасно запускать повторно.
-- ============================================================================
alter table public.tasks alter column sort_order type double precision;
