# Образец нужных ключей. Скопируй этот файл в .env.local и впиши значения
# из своего проекта Supabase (Settings → API). Значения появятся на шаге 3.
# Файл .env.local в GitHub НЕ попадает — он в .gitignore.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
